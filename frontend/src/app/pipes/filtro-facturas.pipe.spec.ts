import { FiltroFacturasPipe } from './filtro-facturas.pipe';

describe('FiltroFacturasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFacturasPipe();
    expect(pipe).toBeTruthy();
  });
});
